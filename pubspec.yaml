name: auto_moto_dash
description: >-
  Motorcycle/car dashboard Flutter package (EN/中文 docs): digital HUD,
  tech analog clusters, weather backgrounds, and inertial particle effects.
version: 0.4.0
homepage: https://github.com/whevether/auto_moto_dash
repository: https://github.com/whevether/auto_moto_dash

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/weather_bg/
